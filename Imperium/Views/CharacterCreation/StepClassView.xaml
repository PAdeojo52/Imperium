<UserControl x:Class="Imperium.Views.CharacterCreation.StepClassView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">

    <Border Style="{StaticResource CardStyle}">
        <DockPanel>
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
                <TextBlock Text="CHOOSE YOUR CLASS" Style="{StaticResource PageHeaderStyle}" 
                           HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <TextBlock Text="Your fighting style defines how you survive the arena."
                           Style="{StaticResource SubtitleStyle}"/>
            </StackPanel>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="350"/>
                </Grid.ColumnDefinitions>

                <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="ClassList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource SelectableCardStyle}" 
                                        MouseLeftButtonDown="ClassCard_Click" Tag="{Binding Name}">
                                    <StackPanel Orientation="Horizontal">
                                        <Border Width="40" Height="40" CornerRadius="20" 
                                                Background="#2E4057" Margin="0,0,12,0">
                                            <iconPacks:PackIconMaterial Kind="Sword" 
                                                                        Width="20" Height="20"
                                                                        Foreground="{StaticResource TextMutedBrush}"
                                                                        HorizontalAlignment="Center" 
                                                                        VerticalAlignment="Center"/>
                                        </Border>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" 
                                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                                            <TextBlock Text="{Binding ShortDesc}" FontSize="11" 
                                                       Foreground="{StaticResource TextMutedBrush}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Border Grid.Column="1" Background="{StaticResource BgInputBrush}" 
                        CornerRadius="8" Padding="20" Margin="12,0,0,0">
                    <StackPanel>
                        <TextBlock x:Name="ClassDetailName" Text="Select a Class" 
                                   FontSize="20" FontWeight="Bold" 
                                   Foreground="{StaticResource AccentGoldBrush}" Margin="0,0,0,12"/>
                        <TextBlock x:Name="ClassDetailDesc" 
                                   Text="Choose a class from the list to see details." 
                                   Style="{StaticResource DescriptionStyle}" Margin="0,0,0,20"/>

                        <TextBlock Text="SPECIALIZATIONS" Style="{StaticResource SectionHeaderStyle}"/>
                        <TextBlock x:Name="ClassDetailSpecs" Text="—" 
                                   FontSize="13" Foreground="{StaticResource AccentBlueBrush}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </Grid>
        </DockPanel>
    </Border>
</UserControl>
